# POST endpoint for the contact form (Formspree, Make/n8n webhook, or your own API).
# Receives JSON: { name, email, company_size, bottleneck, language }.
# If unset, the form falls back to opening a prefilled email to hello@aether.studio.
VITE_CONTACT_ENDPOINT=
